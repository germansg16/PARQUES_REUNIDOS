import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary
  static const neonGreen = Color(0xFF00E676);
  static const neonGreenDark = Color(0xFF00C853);
  static const neonGreenGlow = Color(0x4400E676);

  // Background
  static const bgDark = Color(0xFF0D1117);
  static const bgCard = Color(0xFF161B22);
  static const bgCardLight = Color(0xFF1E2530);

  // Text
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFF8B949E);
  static const textMuted = Color(0xFF484F58);

  // Accents
  static const accentYellow = Color(0xFFFFD700);
  static const accentBlue = Color(0xFF58A6FF);
  static const accentRed = Color(0xFFFF6B6B);
  static const accentPink = Color(0xFFFF4D8D);
  static const accentPurple = Color(0xFFBD2FFF);

  // Wheel segments
  static const wheelRed = Color(0xFFFF4444);
  static const wheelYellow = Color(0xFFFFCC00);
  static const wheelOrange = Color(0xFFFF8C00);
  static const wheelGreen = Color(0xFF00C853);
  static const wheelBlue = Color(0xFF1565C0);
  static const wheelPurple = Color(0xFF7B1FA2);

  // Mission card gradient
  static const missionGradientStart = Color(0xFF0D2B1A);
  static const missionGradientEnd = Color(0xFF0A1F12);

  // Bottom nav
  static const navBg = Color(0xFF161B22);
  static const navBorder = Color(0xFF30363D);
}
